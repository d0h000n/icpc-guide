from django.apps import AppConfig


class ProblemsetsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.problemsets"
    label = "problemsets"
    verbose_name = "Problem sets"
