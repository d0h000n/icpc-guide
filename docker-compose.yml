services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ps_tracker
      POSTGRES_PASSWORD: ps_tracker
      POSTGRES_DB: ps_tracker
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ps_tracker"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pg_data:
